#include <stdio.h>

/**
 * main - return the name of the program
 * @argc: array size
 * @argv: pointer of array
 * Return: 0 - success
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
