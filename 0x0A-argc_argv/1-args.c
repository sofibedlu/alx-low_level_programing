#include <stdio.h>

/**
 * main - print number of argument passed into it
 * @argc: array size
 * @argv: pointer to string
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc);
	return (0);
}
